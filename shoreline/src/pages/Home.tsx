import React from "react";
import Header from "../components/Header";

const Home: React.FunctionComponent = (props) => {
  return (
    <div>
      <Header name="tanreet" />
    </div>
  );
};

export default Home;
